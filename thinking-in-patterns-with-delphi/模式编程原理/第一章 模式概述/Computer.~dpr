program Computer;

uses
  Forms,
  UserForm in 'UserForm.pas' {Client},
  computerClass in 'computerClass.PAS';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TClient, Client);
  Application.Run;
end.
